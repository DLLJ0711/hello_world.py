# Assign a string to variable title
title = "Green Lantern Corps"
# Assign int 0 to counter
counter = 0
# loop over the length of the title
while counter < len(title):
    if (counter % 2) == 1 or title[counter ] != ' ':
        # counter value